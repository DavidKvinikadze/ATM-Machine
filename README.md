A simple ATM machine project, written in python, without using OOP. This project was a final project for a base level(150) python course.

Following code supports the functionalities: withdrawing money, changing currency, validating card. 
Validation is based on a simple logic, by comparing a user input with a pre-existing record which is sotred in a list.
